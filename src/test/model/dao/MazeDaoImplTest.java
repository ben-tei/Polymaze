package test.model.dao;

import org.junit.Test;

public class MazeDaoImplTest {

	@Test
	public void getMazeByNameTest() {
		/*String name = "MuscuMaze";
		Maze maze = DataBaseFacade.getMazeByName(name);
		
		assertNotNull(maze);
		assertEquals(maze.getId(), Integer.valueOf(2));
		assertEquals(maze.getName(), "MuscuMaze");
		assertEquals(maze.getLength(), Integer.valueOf(45));
		assertEquals(maze.getWidth(), Integer.valueOf(25));
		assertEquals(maze.getCreationDate(), java.sql.Date.valueOf("2016-10-09"));
		assertEquals(maze.getCreator().getId(), Integer.valueOf(2));
		assertEquals(maze.getCreator().getName(), "Benji");*/
	}

}
